<form method="post" action="{{ path('app_examen_clinique_delete', {'id': examen_clinique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_clinique.id) }}">
    <button class="btn btn-lg btn-danger">Delete</button>
</form>
