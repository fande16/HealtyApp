{% extends 'base.html.twig' %}

{% block title %}Infirmiere{% endblock %}

{% block content%}
    <h1>Infirmiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ infirmiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ infirmiere.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ infirmiere.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ infirmiere.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ infirmiere.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_infirmiere_index') }}">back to list</a>

    <a href="{{ path('app_infirmiere_edit', {'id': infirmiere.id}) }}"class="btn btn-lg btn-primary">edit</a>

    {{ include('infirmiere/_delete_form.html.twig') }}
{% endblock %}
