{% extends 'base.html.twig' %}

{% block title %}LignePrescription{% endblock %}

{% block content%}
    <h1>LignePrescription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_prescription.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ligne_prescription.Libelle }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_prescription.quantite }}</td>
            </tr>
            <tr>
                <th>Posologie</th>
                <td>{{ ligne_prescription.posologie }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ligne_prescription.date ? ligne_prescription.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne_prescription_index') }}">back to list</a>

    <a href="{{ path('app_ligne_prescription_edit', {'id': ligne_prescription.id}) }}"class="btn btn-lg btn-primary">edit</a>

    {{ include('ligne_prescription/_delete_form.html.twig') }}
{% endblock %}
