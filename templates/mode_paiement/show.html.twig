{% extends 'base.html.twig' %}

{% block title %}ModePaiement{% endblock %}

{% block content%}
    <h1>ModePaiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_paiement.id }}</td>
            </tr>
            <tr>
                <th>TypePaiement</th>
                <td>{{ mode_paiement.typePaiement }}</td>
            </tr>
            <tr>
                <th>Mwthode</th>
                <td>{{ mode_paiement.mwthode }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ mode_paiement.date ? mode_paiement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mode_paiement_index') }}">back to list</a>

    <a href="{{ path('app_mode_paiement_edit', {'id': mode_paiement.id}) }}"class="btn btn-lg btn-primary">edit</a>

    {{ include('mode_paiement/_delete_form.html.twig') }}
{% endblock %}
