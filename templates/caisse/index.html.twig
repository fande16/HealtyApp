{% extends 'base.html.twig' %}

{% block title %}Caisse index{% endblock %}

{% block content%}
    <h1>Caisse index</h1>

    <div class="data-table-container">
    <table id="example" class="table table-striped " style="width:100%">

        <thead>
            <tr>
                <th>Id</th>
                <th>NomCaisse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for caisse in caisses %}
            <tr>
                <td>{{ caisse.id }}</td>
                <td>{{ caisse.nomCaisse }}</td>
                <td>
                    <a href="{{ path('app_caisse_show', {'id': caisse.id}) }}">show</a>
                    <a href="{{ path('app_caisse_edit', {'id': caisse.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

    <a href="{{ path('app_caisse_new') }}"class="btn btn-lg btn-primary">Create new</a>
{% endblock %}
